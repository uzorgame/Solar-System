# Документация по зависимостям проекта

Этот документ описывает все зависимости проекта Solar System и их назначение.

## 📦 Основные зависимости (dependencies)

### three (^0.177.0)

**Что это:**
- Three.js — это мощная библиотека JavaScript для создания и отображения 3D графики в браузере.

**Зачем нужна:**
- Создание 3D сцены с планетами, звездами и космическими объектами
- Управление камерой и освещением
- Загрузка и отображение 3D моделей (GLB/GLTF форматы)
- Рендеринг текстур планет
- Создание орбит и траекторий движения
- Постобработка эффектов (bloom, glow)

**Где используется:**
- `main.js` — вся 3D логика проекта
- Создание сцены, камеры, рендерера
- Загрузка моделей космических зондов
- Анимация движения планет и спутников

**Почему важна:**
Без Three.js невозможно было бы создать интерактивную 3D визуализацию Солнечной системы. Это основа всего проекта.

---

## 🛠️ Зависимости для разработки (devDependencies)

### vite (^7.0.0)

**Что это:**
- Vite — современный инструмент для сборки и разработки веб-приложений.

**Зачем нужна:**
- Быстрый сервер разработки с hot-reload (автоматическое обновление при изменениях)
- Оптимизированная сборка проекта для продакшена
- Обработка модулей ES6+ без дополнительной настройки
- Оптимизация и минификация кода при сборке

**Где используется:**
- Команда `npm run dev` — запускает сервер разработки
- Команда `npm run build` — создает оптимизированную версию в папке `dist/`
- Команда `npm run preview` — предпросмотр собранной версии

**Почему важна:**
Vite обеспечивает быструю разработку и удобную сборку проекта. Без неё пришлось бы настраивать Webpack или другой сборщик вручную.

---

## 📋 Структура зависимостей в node_modules

### Почему node_modules выглядит "перемешанно"?

Папка `node_modules` содержит не только прямые зависимости проекта, но и все их зависимости (транзитивные зависимости). Например:

- `three` → требует множество подпапок для работы
- `vite` → требует множество инструментов для сборки

Каждая библиотека устанавливает свои зависимости, поэтому структура может показаться хаотичной.

### Основные папки в node_modules:

#### @esbuild/
- Инструмент для быстрой компиляции JavaScript
- Используется Vite для оптимизации

#### @rollup/
- Инструмент для сборки модулей
- Используется Vite для создания финальной сборки

#### @types/
- TypeScript определения типов
- Помогает редакторам кода понимать типы данных

#### esbuild/
- Быстрый компилятор JavaScript
- Используется Vite для трансформации кода

#### fdir/
- Библиотека для работы с файловой системой
- Используется Vite для поиска файлов

#### nanoid/
- Генератор уникальных ID
- Используется различными инструментами сборки

#### picocolors/
- Легковесная библиотека для цветного вывода в консоль
- Используется Vite для красивого отображения логов

#### picomatch/
- Библиотека для сопоставления путей файлов (glob patterns)
- Используется Vite для фильтрации файлов

#### postcss/
- Инструмент для обработки CSS
- Используется Vite для работы со стилями

#### rollup/
- Основной инструмент сборки модулей
- Используется Vite для создания финального бандла

#### source-map-js/
- Работа с source maps (для отладки)
- Помогает отслеживать ошибки в исходном коде

#### three/
- Основная библиотека 3D графики
- **Это главная зависимость проекта**

#### tinyglobby/
- Утилита для работы с путями файлов
- Используется инструментами сборки

#### vite/
- Инструмент сборки и разработки
- **Это главная зависимость для разработки**

---

## 🔍 Как понять, что делает каждая зависимость?

### Для обычного пользователя:

1. **three** — делает 3D графику (планеты, звезды, космос)
2. **vite** — помогает запускать и собирать проект

Остальные зависимости — это "внутренние" инструменты, которые нужны для работы этих двух основных библиотек.

### Для разработчика:

Если нужно понять, зачем нужна конкретная зависимость:
1. Откройте `package.json` — там указаны только прямые зависимости
2. Остальные зависимости устанавливаются автоматически как зависимости зависимостей
3. Можно посмотреть в документации каждой библиотеки на npmjs.com

---

## ⚠️ Важно знать

### Не изменяйте node_modules вручную!

- Все изменения будут потеряны при следующей установке зависимостей
- Структура папок критична для работы проекта
- Имена папок должны совпадать с именами пакетов

### Если нужно обновить зависимости:

```bash
# Обновить все зависимости до последних версий
npm update

# Обновить конкретную зависимость
npm install three@latest
npm install vite@latest
```

### Если что-то сломалось:

```bash
# Удалить node_modules и переустановить
rm -rf node_modules package-lock.json
npm install
```

---

## 📚 Полезные ссылки

- [Three.js документация](https://threejs.org/docs/)
- [Vite документация](https://vitejs.dev/)
- [npm документация](https://docs.npmjs.com/)

---

## 📝 Версии зависимостей

Текущие версии указаны в `package.json`:
- `three`: ^0.177.0 (совместимо с версиями 0.177.x)
- `vite`: ^7.0.0 (совместимо с версиями 7.x.x)

Символ `^` означает, что можно устанавливать минорные обновления автоматически.

